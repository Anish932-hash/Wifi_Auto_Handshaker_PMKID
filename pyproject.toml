[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "wifi-auto-handshaker-pmkid"
version = "1.0.0"
description = "Autonomous WiFi 4-way handshake & PMKID capturer with strong verification and adaptive strategy (capture only, no cracking)."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Wifi Auto Handshaker PMKID contributors" }]
keywords = [
    "wifi",
    "security",
    "pentesting",
    "handshake",
    "pmkid",
    "eapol",
    "aircrack-ng",
    "hcxdumptool",
    "audit",
]
classifiers = [
    "Environment :: Console",
    "Intended Audience :: Information Technology",
    "Topic :: Security",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "License :: OSI Approved :: MIT License",
]
dependencies = [
    "PyYAML>=6.0",
    "rich>=13.0",
]

[project.optional-dependencies]
# scapy enables a deeper, independent EAPOL parser fallback (no tshark needed).
full = ["scapy>=2.5.0"]

[project.scripts]
handshaker = "handshaker.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["handshaker*"]
